#include <stdio.h>
int main(void){
    int n;
    scanf("%d",&n);
    int arr[n];
    for (i=0;i<n;i++){
        scanf("%d ",arr[i]);
    }
    int k;
    Scanf("%d",&k);
    for (inti=0;i<n;i++)
    printf("%d",arr[i]); 
return 0;
}