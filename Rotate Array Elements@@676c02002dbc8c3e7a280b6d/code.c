#include <stdio.h>
int main(void){
    int n;
    scanf("%d",&n);
    int arr[n];
    for (i=0;i<n;i++){
        scanf("%d ",arr[i]);
    }
    int k;
    Scanf("%d",&k);
    for (int l=0;l<n;l++)
    printf("%d",arr[i]); 
return 0;
}